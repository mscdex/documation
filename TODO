Compound files
- DIF sector parsing
- dictionary property parsing (property id 0)
- sanity checks with sector counts, etc
- VT_VECTOR property type
- stream property codepage recognition (assumes utf8 currently)
- it seems the value for PID_EDITTIME is that of PID_LASTSAVE_DTM if
  PID_EDITTIME has some weird value (e.g. negative value, etc)? Or
  perhaps PID_EDITTIME is instead "total edit time" as suggested by
  the Apache POI HPSF documentation
- write and modify capabilities

Word documents
- Everything

Excel spreadsheets
- Everything

Powerpoint presentations
- Everything

Outlook messages
- Everything

Visio documents
- Everything

Publisher documents
- Everything

Other
- OOXML parsing
- write tests